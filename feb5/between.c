
// Write a function that takes three arguments x, y, z amd returns
// true if x < y < z, false otherwise
int between(int x, int y, int z) {
   if (x < y && y < z) 
        return 1;
   else
        return 0;
} 
